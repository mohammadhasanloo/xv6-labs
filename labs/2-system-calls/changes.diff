diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/.cvsignore 2-system-calls/.cvsignore
--- xv6/.cvsignore	2020-08-10 14:28:02
+++ 2-system-calls/.cvsignore	1969-12-31 19:00:00
@@ -1,16 +0,0 @@
-*.asm
-*.d
-*.sym
-_*
-kernel
-user1
-userfs
-usertests
-xv6.img
-vectors.S
-bochsout.txt
-bootblock
-bootother
-bootother.out
-parport.out
-fmt
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/.dir-locals.el 2-system-calls/.dir-locals.el
--- xv6/.dir-locals.el	2020-08-10 14:28:02
+++ 2-system-calls/.dir-locals.el	1969-12-31 19:00:00
@@ -1,4 +0,0 @@
-((c-mode
-  (indent-tabs-mode . nil)
-  (c-file-style . "bsd")
-  (c-basic-offset . 2)))
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/.gitignore 2-system-calls/.gitignore
--- xv6/.gitignore	2020-08-10 14:28:02
+++ 2-system-calls/.gitignore	1969-12-31 19:00:00
@@ -1,16 +0,0 @@
-*~
-_*
-*.o
-*.d
-*.asm
-*.sym
-*.img
-vectors.S
-bootblock
-entryother
-initcode
-initcode.out
-kernel
-kernelmemfs
-mkfs
-.gdbinit
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/.idea/.gitignore 2-system-calls/.idea/.gitignore
--- xv6/.idea/.gitignore	1969-12-31 19:00:00
+++ 2-system-calls/.idea/.gitignore	2026-09-06 23:43:20
@@ -0,0 +1,8 @@
+# Default ignored files
+/shelf/
+/workspace.xml
+# Editor-based HTTP Client requests
+/httpRequests/
+# Datasource local storage ignored files
+/dataSources/
+/dataSources.local.xml
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/.idea/Code.iml 2-system-calls/.idea/Code.iml
--- xv6/.idea/Code.iml	1969-12-31 19:00:00
+++ 2-system-calls/.idea/Code.iml	2026-09-06 23:43:20
@@ -0,0 +1,8 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<module type="PYTHON_MODULE" version="4">
+  <component name="NewModuleRootManager">
+    <content url="file://$MODULE_DIR$" />
+    <orderEntry type="inheritedJdk" />
+    <orderEntry type="sourceFolder" forTests="false" />
+  </component>
+</module>
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/.idea/inspectionProfiles/Project_Default.xml 2-system-calls/.idea/inspectionProfiles/Project_Default.xml
--- xv6/.idea/inspectionProfiles/Project_Default.xml	1969-12-31 19:00:00
+++ 2-system-calls/.idea/inspectionProfiles/Project_Default.xml	2026-09-06 23:43:20
@@ -0,0 +1,7 @@
+<component name="InspectionProjectProfileManager">
+  <profile version="1.0">
+    <option name="myName" value="Project Default" />
+    <inspection_tool class="Eslint" enabled="true" level="WARNING" enabled_by_default="true" />
+    <inspection_tool class="Stylelint" enabled="true" level="ERROR" enabled_by_default="true" />
+  </profile>
+</component>
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/.idea/inspectionProfiles/profiles_settings.xml 2-system-calls/.idea/inspectionProfiles/profiles_settings.xml
--- xv6/.idea/inspectionProfiles/profiles_settings.xml	1969-12-31 19:00:00
+++ 2-system-calls/.idea/inspectionProfiles/profiles_settings.xml	2026-09-06 23:43:20
@@ -0,0 +1,6 @@
+<component name="InspectionProjectProfileManager">
+  <settings>
+    <option name="USE_PROJECT_PROFILE" value="false" />
+    <version value="1.0" />
+  </settings>
+</component>
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/.idea/misc.xml 2-system-calls/.idea/misc.xml
--- xv6/.idea/misc.xml	1969-12-31 19:00:00
+++ 2-system-calls/.idea/misc.xml	2026-09-06 23:43:20
@@ -0,0 +1,4 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project version="4">
+  <component name="ProjectRootManager" version="2" project-jdk-name="Python 3.8" project-jdk-type="Python SDK" />
+</project>
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/.idea/modules.xml 2-system-calls/.idea/modules.xml
--- xv6/.idea/modules.xml	1969-12-31 19:00:00
+++ 2-system-calls/.idea/modules.xml	2026-09-06 23:43:20
@@ -0,0 +1,8 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project version="4">
+  <component name="ProjectModuleManager">
+    <modules>
+      <module fileurl="file://$PROJECT_DIR$/.idea/Code.iml" filepath="$PROJECT_DIR$/.idea/Code.iml" />
+    </modules>
+  </component>
+</project>
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/.idea/vcs.xml 2-system-calls/.idea/vcs.xml
--- xv6/.idea/vcs.xml	1969-12-31 19:00:00
+++ 2-system-calls/.idea/vcs.xml	2026-09-06 23:43:20
@@ -0,0 +1,6 @@
+<?xml version="1.0" encoding="UTF-8"?>
+<project version="4">
+  <component name="VcsDirectoryMappings">
+    <mapping directory="$PROJECT_DIR$" vcs="Git" />
+  </component>
+</project>
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/Makefile 2-system-calls/Makefile
--- xv6/Makefile	2020-08-10 14:28:02
+++ 2-system-calls/Makefile	2026-09-06 23:43:20
@@ -166,6 +166,7 @@
 .PRECIOUS: %.o
 
 UPROGS=\
+	_sort_string\
 	_cat\
 	_echo\
 	_forktest\
@@ -181,6 +182,11 @@
 	_usertests\
 	_wc\
 	_zombie\
+	_wait_for_process_test\
+	_find_next_prime_number_interface\
+	_get_call_count\
+	_get_most_caller\
+	_getpid_gdb\
 
 fs.img: mkfs README $(UPROGS)
 	./mkfs fs.img README $(UPROGS)
@@ -249,8 +255,13 @@
 
 EXTRA=\
 	mkfs.c ulib.c user.h cat.c echo.c forktest.c grep.c kill.c\
-	ln.c ls.c mkdir.c rm.c stressfs.c usertests.c wc.c zombie.c\
+	ln.c ls.c sort_string.c mkdir.c rm.c stressfs.c usertests.c wc.c zombie.c\
 	printf.c umalloc.c\
+	get_call_count.c\
+	get_most_caller.c\
+	wait_for_process_test.c\
+	find_next_prime_number_interface.c\
+	getpid_gdb.c\
 	README dot-bochsrc *.pl toc.* runoff runoff1 runoff.list\
 	.gdbinit.tmpl gdbutil\
 
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/console.c 2-system-calls/console.c
--- xv6/console.c	2020-08-10 14:28:02
+++ 2-system-calls/console.c	2026-09-06 23:43:20
@@ -15,6 +15,11 @@
 #include "proc.h"
 #include "x86.h"
 
+#define UP_ARROW 226
+#define DOWN_ARROW 227
+#define LEFT_ARROW 228
+#define RIGHT_ARROW 229
+
 static void consputc(int);
 
 static int panicked = 0;
@@ -139,12 +144,19 @@
   outb(CRTPORT, 15);
   pos |= inb(CRTPORT+1);
 
-  if(c == '\n')
-    pos += 80 - pos%80;
-  else if(c == BACKSPACE){
-    if(pos > 0) --pos;
-  } else
-    crt[pos++] = (c&0xff) | 0x0700;  // black on white
+  switch(c) {
+    case '\n':
+      pos += 80 - pos%80;
+      break;
+    case BACKSPACE:
+      if(pos > 0) --pos;
+      break;
+    case LEFT_ARROW:
+      if(pos > 0) --pos;
+      break;
+    default:
+      crt[pos++] = (c&0xff) | 0x0700;  // black on white
+  }
 
   if(pos < 0 || pos > 25*80)
     panic("pos under/overflow");
@@ -159,7 +171,8 @@
   outb(CRTPORT+1, pos>>8);
   outb(CRTPORT, 15);
   outb(CRTPORT+1, pos);
-  crt[pos] = ' ' | 0x0700;
+  if (c == BACKSPACE)
+    crt[pos] = ' ' | 0x0700;
 }
 
 void
@@ -171,28 +184,202 @@
       ;
   }
 
-  if(c == BACKSPACE){
-    uartputc('\b'); uartputc(' '); uartputc('\b');
-  } else
-    uartputc(c);
+  switch (c) {
+    case BACKSPACE:
+      // uartputc prints to Linux's terminal
+      uartputc('\b'); uartputc(' '); uartputc('\b');  // uart is writing to the linux shell
+      break;
+    case LEFT_ARROW:
+      uartputc('\b');
+      break;
+    default:
+      uartputc(c);
+  }
+  // cgaputc prints to QEMU's terminal
   cgaputc(c);
 }
 
 #define INPUT_BUF 128
+#define MAX_HISTORY 20
 struct {
   char buf[INPUT_BUF];
   uint r;  // Read index
   uint w;  // Write index
   uint e;  // Edit index
+  uint rightmost; // the first empty char in the line
 } input;
 
+char charsToBeMoved[INPUT_BUF];  // temporary storage for input.buf in a certain context
+
+/*
+  this struct will hold the history buffer array
+*/
+struct {
+  char bufferArr[MAX_HISTORY][INPUT_BUF]; //holds the actual command strings -
+  uint lengthsArr[MAX_HISTORY]; // this will hold the length of each command string
+  uint lastCommandIndex;  //the index of the last command entered to history
+  int numOfCommmandsInMem; //number of history commands in mem
+  int currentHistory;//this will hold the current history view (the oldest will be MAX_HISTORY-1)
+} historyBufferArray;
+
+char oldBuf[INPUT_BUF];// this will hold the details of the command that was written before accessing the history
+uint lengthOfOldBuf;
+
+char buf2[INPUT_BUF];
+
 #define C(x)  ((x)-'@')  // Control-x
 
+/*
+Copy input.buf to a safe location. Used only when punching in new keys and the
+caret isn't at the end of the line.
+*/
+void copyCharsToBeMoved() {
+  uint n = input.rightmost - input.r;
+  uint i;
+  for (i = 0; i < n; i++)
+    charsToBeMoved[i] = input.buf[(input.e + i) % INPUT_BUF];
+}
+
+/*
+Shift input.buf one byte to the left, and repaint the chars on-screen. Used
+Used only when punching in BACKSPACE and the caret isn't at the end of the line.
+*/
+void shiftbufleft() {
+  uint n = input.rightmost - input.e;
+  uint i;
+  consputc(LEFT_ARROW);
+  input.e--;
+  for (i = 0; i < n; i++) {
+    char c = input.buf[(input.e + i + 1) % INPUT_BUF];
+    input.buf[(input.e + i) % INPUT_BUF] = c;
+    consputc(c);
+  }
+  input.rightmost--;
+  consputc(' '); // delete the last char in line
+  for (i = 0; i <= n; i++) {
+    consputc(LEFT_ARROW); // shift the caret back to the left
+  }
+}
+
+/*
+Shift input.buf one byte to the right, and repaint the chars on-screen. Used
+Used only when punching in new keys and the caret isn't at the end of the line.
+*/
+void shiftbufright() {
+  uint n = input.rightmost - input.e;
+  int i;
+  for (i = 0; i < n; i++) {
+
+    char c = charsToBeMoved[i];
+    input.buf[(input.e + i) % INPUT_BUF] = c;
+    consputc(c);
+  }
+  // reset charsToBeMoved for future use
+  memset(charsToBeMoved, '\0', INPUT_BUF);
+  // return the caret to its correct position
+  for (i = 0; i < n; i++) {
+    consputc(LEFT_ARROW);
+  }
+}
+
+/*
+  this method eareases the current line from screen
+*/
 void
+earaseCurrentLineOnScreen(void){
+    uint numToEarase = input.rightmost - input.r;
+    uint i;
+    for (i = 0; i < numToEarase; i++) {
+      consputc(BACKSPACE);
+    }
+}
+
+/*
+  this method copies the chars currently on display (and on Input.buf) to oldBuf and save its length on current_history_viewed.lengthOld
+*/
+void
+copyCharsToBeMovedToOldBuf(void){
+    lengthOfOldBuf = input.rightmost - input.r;
+    uint i;
+    for (i = 0; i < lengthOfOldBuf; i++) {
+        oldBuf[i] = input.buf[(input.r+i)%INPUT_BUF];
+    }
+
+}
+
+/*
+  this method earase all the content of the current command on the inputbuf
+*/
+void
+earaseContentOnInputBuf(){
+  input.rightmost = input.r;
+  input.e = input.r;
+}
+
+/*
+  this method will print the given buf on the screen
+*/
+void
+copyBufferToScreen(char * bufToPrintOnScreen, uint length){
+  uint i;
+  for (i = 0; i < length; i++) {
+    consputc(bufToPrintOnScreen[i]);
+  }
+}
+
+/*
+  this method will copy the given buf to Input.buf
+  will set the input.e and input.rightmost
+  assumes input.r=input.w=input.rightmost=input.e
+*/
+void
+copyBufferToInputBuf(char * bufToSaveInInput, uint length){
+  uint i;
+  for (i = 0; i < length; i++) {
+    input.buf[(input.r+i)%INPUT_BUF] = bufToSaveInInput[i];
+  }
+  input.e = input.r+length;
+  input.rightmost = input.e;
+}
+
+/*
+  this method copies the current command in the input.buf to the saved history
+  @param length - length of command to be saved
+*/
+void
+saveCommandInHistory(){
+  historyBufferArray.currentHistory= -1;//reseting the users history current viewed
+  if (historyBufferArray.numOfCommmandsInMem < MAX_HISTORY)
+    historyBufferArray.numOfCommmandsInMem++; //when we get to MAX_HISTORY commands in memory we keep on inserting to the array in a circular mution
+  uint l = input.rightmost-input.r -1;
+  historyBufferArray.lastCommandIndex = (historyBufferArray.lastCommandIndex - 1)%MAX_HISTORY;
+  historyBufferArray.lengthsArr[historyBufferArray.lastCommandIndex] = l;
+  uint i;
+  for (i = 0; i < l; i++) { //do not want to save in memory the last char '/n'
+    historyBufferArray.bufferArr[historyBufferArray.lastCommandIndex][i] =  input.buf[(input.r+i)%INPUT_BUF];
+  }
+
+}
+
+/*
+  this is the function that gets called by the sys_history and writes the requested command history in the buffer
+*/
+int history(char *buffer, int historyId) {
+  if (historyId < 0 || historyId > MAX_HISTORY - 1)
+    return -2;
+  if (historyId >= historyBufferArray.numOfCommmandsInMem )
+    return -1;
+  memset(buffer, '\0', INPUT_BUF);
+  int tempIndex = (historyBufferArray.lastCommandIndex + historyId) % MAX_HISTORY;
+  memmove(buffer, historyBufferArray.bufferArr[tempIndex], historyBufferArray.lengthsArr[tempIndex]);
+  return 0;
+}
+
+void
 consoleintr(int (*getc)(void))
 {
   int c, doprocdump = 0;
-
+  int tempIndex;
   acquire(&cons.lock);
   while((c = getc()) >= 0){
     switch(c){
@@ -201,29 +388,125 @@
       doprocdump = 1;
       break;
     case C('U'):  // Kill line.
-      while(input.e != input.w &&
-            input.buf[(input.e-1) % INPUT_BUF] != '\n'){
-        input.e--;
-        consputc(BACKSPACE);
-      }
-      break;
+     if (input.rightmost > input.e) { // caret isn't at the end of the line
+          uint numtoshift = input.rightmost - input.e;
+          uint placestoshift = input.e - input.w;
+          uint i;
+          for (i = 0; i < placestoshift; i++) {
+            consputc(LEFT_ARROW);
+          }
+          memset(buf2, '\0', INPUT_BUF);
+          for (i = 0; i < numtoshift; i++) {
+            buf2[i] = input.buf[(input.w + i + placestoshift) % INPUT_BUF];
+          }
+          for (i = 0; i < numtoshift; i++) {
+            input.buf[(input.w + i) % INPUT_BUF] = buf2[i];
+          }
+          input.e -= placestoshift;
+          input.rightmost -= placestoshift;
+          for (i = 0; i < numtoshift; i++) { // repaint the chars
+            consputc(input.buf[(input.e + i) % INPUT_BUF]);
+          }
+          for (i = 0; i < placestoshift; i++) { // erase the leftover chars
+            consputc(' ');
+          }
+          for (i = 0; i < placestoshift + numtoshift; i++) { // move the caret back to the left
+            consputc(LEFT_ARROW);
+          }
+        }
+        else { // caret is at the end of the line -                                       ( deleting everything from both screen and inputbuf)
+          while(input.e != input.w &&
+                input.buf[(input.e - 1) % INPUT_BUF] != '\n'){
+            input.e--;
+            input.rightmost--;
+            consputc(BACKSPACE);
+          }
+        }
+        break;
     case C('H'): case '\x7f':  // Backspace
-      if(input.e != input.w){
-        input.e--;
-        consputc(BACKSPACE);
-      }
-      break;
+      if (input.rightmost != input.e && input.e != input.w) { // caret isn't at the end of the line
+          shiftbufleft();
+          break;
+        }
+        if(input.e != input.w){ // caret is at the end of the line - deleting last char
+          input.e--;
+          input.rightmost--;
+          consputc(BACKSPACE);
+        }
+        break;
+    case LEFT_ARROW:
+        if (input.e != input.w) {
+          input.e--;
+          consputc(c);
+        }
+        break;
+    case RIGHT_ARROW:
+        if (input.e < input.rightmost) {
+          consputc(input.buf[input.e % INPUT_BUF]);
+          input.e++;
+        }
+        else if (input.e == input.rightmost){
+          consputc(' ');
+          consputc(LEFT_ARROW);
+        }
+        break;
+    case UP_ARROW:
+       if (historyBufferArray.currentHistory < historyBufferArray.numOfCommmandsInMem-1 ){ // current history means the oldest possible will be MAX_HISTORY-1
+          earaseCurrentLineOnScreen();
+          if (historyBufferArray.currentHistory == -1)
+              copyCharsToBeMovedToOldBuf();
+          earaseContentOnInputBuf();
+          historyBufferArray.currentHistory++;
+          tempIndex = (historyBufferArray.lastCommandIndex + historyBufferArray.currentHistory) %MAX_HISTORY;
+          copyBufferToScreen(historyBufferArray.bufferArr[ tempIndex]  , historyBufferArray.lengthsArr[tempIndex]);
+          copyBufferToInputBuf(historyBufferArray.bufferArr[ tempIndex]  , historyBufferArray.lengthsArr[tempIndex]);
+        }
+        break;
+      case DOWN_ARROW:
+        switch(historyBufferArray.currentHistory){
+          case -1:
+            //does nothing
+            break;
+          case 0: //get string from old buf
+            earaseCurrentLineOnScreen();
+            copyBufferToInputBuf(oldBuf, lengthOfOldBuf);
+            copyBufferToScreen(oldBuf, lengthOfOldBuf);
+            historyBufferArray.currentHistory--;
+            break;
+          default:
+            earaseCurrentLineOnScreen();
+            historyBufferArray.currentHistory--;
+            tempIndex = (historyBufferArray.lastCommandIndex + historyBufferArray.currentHistory)%MAX_HISTORY;
+            copyBufferToScreen(historyBufferArray.bufferArr[ tempIndex]  , historyBufferArray.lengthsArr[tempIndex]);
+            copyBufferToInputBuf(historyBufferArray.bufferArr[ tempIndex]  , historyBufferArray.lengthsArr[tempIndex]);
+            break;
+        }
+        break;
+    case '\r':
+          input.e = input.rightmost;
+  
     default:
-      if(c != 0 && input.e-input.r < INPUT_BUF){
-        c = (c == '\r') ? '\n' : c;
-        input.buf[input.e++ % INPUT_BUF] = c;
-        consputc(c);
-        if(c == '\n' || c == C('D') || input.e == input.r+INPUT_BUF){
-          input.w = input.e;
-          wakeup(&input.r);
+        if(c != 0 && input.e-input.r < INPUT_BUF){
+          c = (c == '\r') ? '\n' : c;
+          if (input.rightmost > input.e) { // caret isn't at the end of the line
+            copyCharsToBeMoved();
+            input.buf[input.e++ % INPUT_BUF] = c;
+            input.rightmost++;
+            consputc(c);
+            shiftbufright();
+          }
+          else {
+            input.buf[input.e++ % INPUT_BUF] = c;
+            input.rightmost = input.e - input.rightmost == 1 ? input.e : input.rightmost;
+            consputc(c);
+          }
+          if(c == '\n' || c == C('D') || input.rightmost == input.r + INPUT_BUF){
+            saveCommandInHistory();
+            input.w = input.rightmost;
+            wakeup(&input.r);
+          }
         }
-      }
-      break;
+        break;
     }
   }
   release(&cons.lock);
@@ -295,5 +578,8 @@
   cons.locking = 1;
 
   ioapicenable(IRQ_KBD, 0);
+
+  historyBufferArray.numOfCommmandsInMem=0;
+  historyBufferArray.lastCommandIndex=0;
 }
 
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/defs.h 2-system-calls/defs.h
--- xv6/defs.h	2020-08-10 14:28:02
+++ 2-system-calls/defs.h	2026-09-06 23:43:20
@@ -118,8 +118,12 @@
 void            sleep(void*, struct spinlock*);
 void            userinit(void);
 int             wait(void);
+
+int             wait_for_process(int);
+
 void            wakeup(void*);
 void            yield(void);
+int             find_next_prime_number(int);
 
 // swtch.S
 void            swtch(struct context**, struct context*);
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/find_next_prime_number_interface.c 2-system-calls/find_next_prime_number_interface.c
--- xv6/find_next_prime_number_interface.c	1969-12-31 19:00:00
+++ 2-system-calls/find_next_prime_number_interface.c	2026-09-06 23:43:20
@@ -0,0 +1,20 @@
+#include "types.h"
+#include "stat.h"
+#include "user.h"
+
+int main(int argc, char *argv[])
+{
+
+        int saved_ebx, number = atoi(argv[1]);
+        // we can add assembly instructions using asm
+		asm volatile(
+			"movl %%ebx, %0;"
+			"movl %1, %%ebx;"
+			: "=r" (saved_ebx)
+			: "r"(number)
+		);
+
+		printf(1, "The next prime number is:  %d\n", find_next_prime_number());
+		asm("movl %0, %%ebx" : : "r"(saved_ebx));
+		exit();
+}
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/get_call_count.c 2-system-calls/get_call_count.c
--- xv6/get_call_count.c	1969-12-31 19:00:00
+++ 2-system-calls/get_call_count.c	2026-09-06 23:43:20
@@ -0,0 +1,20 @@
+#include "types.h"
+#include "user.h"
+#include "syscall.h"
+
+int
+main(int argc, char *argv[])
+{
+  if (fork() == 0) {
+    printf(1, "child fork count %d\n", get_call_count(SYS_fork));
+    printf(1, "child write count %d\n", get_call_count(SYS_write));
+	printf(1, "child get_call_count count %d\n", get_call_count(SYS_get_call_count));
+  } else {
+    wait();
+    printf(1, "parent fork count %d\n", get_call_count(SYS_fork));
+    printf(1, "parent write count %d\n", get_call_count(SYS_write));
+	printf(1, "parent get_call_count count %d\n", get_call_count(SYS_get_call_count));
+  }
+  printf(1, "wait count %d\n", get_call_count(SYS_wait));
+  exit();
+}
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/get_most_caller.c 2-system-calls/get_most_caller.c
--- xv6/get_most_caller.c	1969-12-31 19:00:00
+++ 2-system-calls/get_most_caller.c	2026-09-06 23:43:20
@@ -0,0 +1,14 @@
+#include "types.h"
+#include "stat.h"
+#include "user.h"
+#include "syscall.h"
+
+
+int main(void) 
+{
+    printf(1, "pid of the most caller(wait): %d\n", get_most_caller(SYS_wait));
+    printf(1, "pid of the most caller(fork): %d\n", get_most_caller(SYS_fork));
+    printf(1, "pid of the most caller(wrtie): %d\n", get_most_caller(SYS_write));
+
+    exit();
+} 
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/getpid_gdb.c 2-system-calls/getpid_gdb.c
--- xv6/getpid_gdb.c	1969-12-31 19:00:00
+++ 2-system-calls/getpid_gdb.c	2026-09-06 23:43:20
@@ -0,0 +1,9 @@
+#include "types.h"
+#include "stat.h"
+#include "user.h"
+
+int main(int argc, char *argv[])
+{
+		printf(1, "GETPID() IS CALLED! %d\n", getpid());
+		exit();
+}
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/init.c 2-system-calls/init.c
--- xv6/init.c	2020-08-10 14:28:02
+++ 2-system-calls/init.c	2026-09-06 23:43:20
@@ -21,6 +21,10 @@
 
   for(;;){
     printf(1, "init: starting sh\n");
+    printf(1, "Group #22:\n");
+    printf(1, "1. Danial Saeedi\n");
+    printf(1, "2. Soroosh Sadeghian\n");
+    printf(1, "3. Mohammad GharehHasanloo\n");    
     pid = fork();
     if(pid < 0){
       printf(1, "init: fork failed\n");
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/proc.c 2-system-calls/proc.c
--- xv6/proc.c	2020-08-10 14:28:02
+++ 2-system-calls/proc.c	2026-09-06 23:43:20
@@ -112,6 +112,16 @@
   memset(p->context, 0, sizeof *p->context);
   p->context->eip = (uint)forkret;
 
+  for(int i = 0; i < sizeof(p->callcount)/sizeof(int); i++){
+    p->callcount[i] = 0;
+  }
+
+  for(int i = 0; i < 100; i++){
+    for(int j = 0; j < 100; j++){
+      p->caller_count[i][j] = 0;
+    }
+  }
+
   return p;
 }
 
@@ -251,6 +261,8 @@
 
   // Parent might be sleeping in wait().
   wakeup1(curproc->parent);
+  wakeup1(curproc->waiting_processor);
+  curproc->waiting_processor = '\0';
 
   // Pass abandoned children to init.
   for(p = ptable.proc; p < &ptable.proc[NPROC]; p++){
@@ -531,4 +543,81 @@
     }
     cprintf("\n");
   }
-}
+}
+
+int wait_for_process(int pid) {
+  struct proc *p;
+  struct proc *curproc = myproc();
+  int pidFound = 0;
+
+  acquire(&ptable.lock);
+
+  for(;;) {
+    for(p = ptable.proc; p < &ptable.proc[NPROC]; p++) {
+      if (p->pid != pid){
+        continue;
+      }
+      p->waiting_processor = curproc;
+      pidFound = 1;
+      if (p->state == ZOMBIE) {
+
+        kfree(p->kstack);
+        p->kstack = 0;
+        freevm(p->pgdir);
+        p->pid = 0;
+        p->parent = 0;
+        p->name[0] = 0;
+        p->killed = 0;
+        p->state = UNUSED;
+        release(&ptable.lock);
+        return pid;
+      }
+    }
+    
+    if (!pidFound || curproc->killed) {
+      release(&ptable.lock);
+      return -1;
+    }
+
+    sleep(curproc, &ptable.lock); // DOC: wait-sleep
+  }
+}
+
+//util function
+int isPrime(int n)
+{
+    // Corner cases
+    if (n <= 1)  return 0;
+    if (n <= 3)  return 1;
+
+    // This is checked so that we can skip
+    // middle five numbers in below loop
+    if (n%2 == 0 || n%3 == 0) return 0;
+
+    for (int i=5; i*i<=n; i=i+6)
+        if (n%i == 0 || n%(i+2) == 0)
+           return 0;
+
+    return 1;
+}
+
+int find_next_prime_number(int n)
+{
+    // Base case
+    if (n <= 1)
+        return 2;
+
+    int prime = n;
+    int found = 0;
+
+    // Loop continuously until isPrime returns
+    // 1 for a number greater than n
+    while (!found) {
+        prime++;
+
+        if (isPrime(prime))
+            found = 1;
+    }
+
+    return prime;
+}
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/proc.h 2-system-calls/proc.h
--- xv6/proc.h	2020-08-10 14:28:02
+++ 2-system-calls/proc.h	2026-09-06 23:43:20
@@ -49,6 +49,9 @@
   struct file *ofile[NOFILE];  // Open files
   struct inode *cwd;           // Current directory
   char name[16];               // Process name (debugging)
+  struct proc *waiting_processor;
+  int callcount[27];	         // System call tracking
+  int caller_count[100][100];	 // most caller system call
 };
 
 // Process memory is laid out contiguously, low addresses first:
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/sort_string.c 2-system-calls/sort_string.c
--- xv6/sort_string.c	1969-12-31 19:00:00
+++ 2-system-calls/sort_string.c	2026-09-06 23:43:20
@@ -0,0 +1,36 @@
+#include "types.h"
+#include "stat.h"
+#include "user.h"
+#include "fcntl.h"
+
+
+
+
+int
+main(int argc, char *argv[])
+{
+  char* curr_string = argv[1];
+  int stringLength = strlen(curr_string);
+  char temp;
+
+  for (int i = 0; i < stringLength - 1; i++) {
+    for (int j = i + 1; j < stringLength; j++) {
+      if (curr_string[i] > curr_string[j]) {
+        temp = curr_string[i];
+        curr_string[i] = curr_string[j];
+        curr_string[j] = temp;
+      }
+    }
+  }
+  
+  int fd;
+  unlink("sort_string.txt");
+  fd = open("sort_string.txt", O_CREATE | O_RDWR);
+  if(fd < 0){
+    printf(1, "Something went wrong in opening file\n");
+  }
+  write(fd, curr_string, stringLength);
+  close(fd);
+  
+  exit();
+}
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/syscall.c 2-system-calls/syscall.c
--- xv6/syscall.c	2020-08-10 14:28:02
+++ 2-system-calls/syscall.c	2026-09-06 23:43:20
@@ -103,6 +103,10 @@
 extern int sys_wait(void);
 extern int sys_write(void);
 extern int sys_uptime(void);
+extern int sys_wait_for_process(void);
+extern int sys_find_next_prime_number(void);
+extern int sys_get_call_count(void);
+extern int sys_get_most_caller(void);
 
 static int (*syscalls[])(void) = {
 [SYS_fork]    sys_fork,
@@ -126,8 +130,13 @@
 [SYS_link]    sys_link,
 [SYS_mkdir]   sys_mkdir,
 [SYS_close]   sys_close,
+[SYS_wait_for_process] sys_wait_for_process,
+[SYS_find_next_prime_number] sys_find_next_prime_number,
+[SYS_get_call_count]	sys_get_call_count,
+[SYS_get_most_caller] sys_get_most_caller,
 };
 
+int caller_count[100][100] = {0};
 void
 syscall(void)
 {
@@ -135,6 +144,16 @@
   struct proc *curproc = myproc();
 
   num = curproc->tf->eax;
+  curproc->callcount[num - 1] = curproc->callcount[num - 1] + 1;
+  
+  caller_count[num - 1][curproc->pid] = caller_count[num - 1][curproc->pid] + 1;
+
+  for(int i = 0;i < 100; i++){
+    for (int j = 0; j < 100; j++){
+        curproc->caller_count[i][j] = caller_count[i][j];
+    }
+  }
+
   if(num > 0 && num < NELEM(syscalls) && syscalls[num]) {
     curproc->tf->eax = syscalls[num]();
   } else {
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/syscall.h 2-system-calls/syscall.h
--- xv6/syscall.h	2020-08-10 14:28:02
+++ 2-system-calls/syscall.h	2026-09-06 23:43:20
@@ -20,3 +20,11 @@
 #define SYS_link   19
 #define SYS_mkdir  20
 #define SYS_close  21
+
+#define SYS_find_next_prime_number 22
+#define SYS_wait_for_process 29
+
+#define SYS_get_call_count 24
+#define SYS_get_most_caller 23
+
+#define SYS_callnumber	  27
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/sysproc.c 2-system-calls/sysproc.c
--- xv6/sysproc.c	2020-08-10 14:28:02
+++ 2-system-calls/sysproc.c	2026-09-06 23:43:20
@@ -89,3 +89,49 @@
   release(&tickslock);
   return xticks;
 }
+
+int
+sys_wait_for_process(void)
+{
+  int pid;
+  if(argint(0, &pid)<0){
+    return -1;
+  }
+  else{
+    return wait_for_process(pid);
+  }
+
+}
+
+int
+sys_find_next_prime_number(void)
+{
+  int number = myproc()->tf->ebx; //register after eax
+  cprintf("Kernel: sys_find_next_prime_number() called for number %d\n", number);
+  return find_next_prime_number(number);
+}
+
+int sys_get_call_count(void){
+  int n;
+  if(argint(0, &n) < 0)
+    return -1;
+  return(myproc()->callcount[n-1]);
+}
+
+int sys_get_most_caller()
+{
+  int n;
+  if(argint(0, &n) < 0)
+    return -1;
+  
+  int index = -1;
+  int max = -1;
+
+  for(int i = 0; i < 100; i++){
+    if(max < myproc()->caller_count[n-1][i]){
+      max = myproc()->caller_count[n-1][i];
+      index = i;
+    }
+  }
+  return index;
+}
\ No newline at end of file
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/user.h 2-system-calls/user.h
--- xv6/user.h	2020-08-10 14:28:02
+++ 2-system-calls/user.h	2026-09-06 23:43:20
@@ -5,6 +5,9 @@
 int fork(void);
 int exit(void) __attribute__((noreturn));
 int wait(void);
+
+int wait_for_process(int);
+
 int pipe(int*);
 int write(int, const void*, int);
 int read(int, void*, int);
@@ -23,6 +26,11 @@
 char* sbrk(int);
 int sleep(int);
 int uptime(void);
+
+int get_call_count(int sys_call);
+int get_most_caller(int sys_call);
+
+int find_next_prime_number(void);
 
 // ulib.c
 int stat(const char*, struct stat*);
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/usertests.c 2-system-calls/usertests.c
--- xv6/usertests.c	2020-08-10 14:28:02
+++ 2-system-calls/usertests.c	2026-09-06 23:43:20
@@ -8,6 +8,7 @@
 #include "traps.h"
 #include "memlayout.h"
 
+
 char buf[8192];
 char name[3];
 char *echoargv[] = { "echo", "ALL", "TESTS", "PASSED", 0 };
@@ -1415,7 +1416,7 @@
 sbrktest(void)
 {
   int fds[2], pid, pids[10], ppid;
-  char *a, *b, *c, *lastaddr, *oldbrk, *p, scratch;
+  char *a, *b, *c, *oldbrk, *p, scratch;
   uint amt;
 
   printf(stdout, "sbrk test\n");
@@ -1457,8 +1458,6 @@
     printf(stdout, "sbrk test failed to grow big address space; enough phys mem?\n");
     exit();
   }
-  lastaddr = (char*) (BIG-1);
-  *lastaddr = 99;
 
   // can one de-allocate?
   a = sbrk(0);
@@ -1478,11 +1477,6 @@
   c = sbrk(4096);
   if(c != a || sbrk(0) != a + 4096){
     printf(stdout, "sbrk re-allocation failed, a %x c %x\n", a, c);
-    exit();
-  }
-  if(*lastaddr == 99){
-    // should be zero
-    printf(stdout, "sbrk de-allocation didn't really deallocate\n");
     exit();
   }
 
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/usys.S 2-system-calls/usys.S
--- xv6/usys.S	2020-08-10 14:28:02
+++ 2-system-calls/usys.S	2026-09-06 23:43:20
@@ -29,3 +29,7 @@
 SYSCALL(sbrk)
 SYSCALL(sleep)
 SYSCALL(uptime)
+SYSCALL(wait_for_process)
+SYSCALL(find_next_prime_number)
+SYSCALL(get_call_count)
+SYSCALL(get_most_caller)
diff -u -r -N --exclude=*.o --exclude=*.d --exclude=*.asm --exclude=*.sym --exclude=*.img --exclude=_* --exclude=.gdbinit --exclude=kernel* --exclude=bootblock* --exclude=entryother --exclude=initcode* --exclude=mkfs --exclude=vectors.S --exclude=.DS_Store xv6/wait_for_process_test.c 2-system-calls/wait_for_process_test.c
--- xv6/wait_for_process_test.c	1969-12-31 19:00:00
+++ 2-system-calls/wait_for_process_test.c	2026-09-06 23:43:20
@@ -0,0 +1,58 @@
+#include "types.h"
+#include "stat.h"
+#include "user.h"
+#include "syscall.h"
+
+void wait_for_process_test(void) {
+    int pid1, pid2;
+    if((pid1 = fork())!=0) // Create a child
+    {
+        // We are in the parent
+        if((pid2 = fork())!=0)
+        {
+            // We are in the parent
+            wait();
+            printf(1, "Second child has finished \n");
+            printf(1, "Parent has been finished\n");
+            exit();    
+        }
+        else
+        {
+            // We are in the 2nd child
+            int termpid = wait_for_process(pid1);
+		    if(termpid > 0){
+			    printf(1, "First child has finished \n");
+		    }         
+		    exit();  
+
+        }    
+    }
+    else
+    {
+        // We are in the 1st child
+        for(int i=0;i<100000000;i++){
+            if(i== 9000){
+                printf(1, "Wait %d iterations for first child\n", i);
+            }
+            else if(i== 900000){
+                printf(1, "Wait %d iterations for first child\n", i);
+            }
+            else if(i== 9000000){
+                printf(1, "Wait %d iterations for first child\n", i);
+            }
+            else if(i== 90000000){
+                printf(1, "Wait %d iterations for first child\n", i);
+            }                        
+        }
+        exit();     
+    }
+}
+
+int
+main(int argc, char *argv[])
+{
+	printf(1, "Testing wait_for_process function\n");
+	wait_for_process_test();
+	
+	exit();
+}
\ No newline at end of file
